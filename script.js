window.onload = function() {

// HOME TAB
    setTimeout(
        function() {
            document.querySelector(".intro-message").style.opacity = "1";
        }, 100
    );

    setTimeout(
        function() {
            document.querySelector(".intro-description").style.opacity = "1";
        }, 800
    );

    setTimeout(
        function() {
            document.querySelector(".intro-browse").style.opacity = "1";
        }, 800
    );

// ABOUT TAB

    setTimeout(
        function() {
            document.querySelector(".about-jful").style.opacity = "1";
        }, 100
    );

    setTimeout(
        function() {
            document.querySelector(".bio").style.opacity = "1";
        }, 500
    );

    setTimeout(
        function() {
            document.querySelector(".donate-text").style.opacity = "1";
        }, 1600
    );

// BROWSE TAB
    setTimeout(
        function() {
            document.querySelector(".jful-x-eido-collection").style.opacity = "1";
        }, 100
    );

    setTimeout(
        function() {
            document.querySelector(".browse-thru").style.opacity = "1";
        }, 800
    );

    setTimeout(
        function() {
            document.querySelector(".browse-note").style.opacity = "1";
        }, 800
    );

// PRICING INFORMATION
    setTimeout(
        function() {
            document.querySelector(".pricing-information").style.opacity = "1";
        }, 100
    );

    setTimeout(
        function() {
            document.querySelector(".purchase-note").style.opacity = "1";
        }, 800
    );

    setTimeout(
        function() {
            document.querySelector("h3.instructions").style.opacity = "1";
        }, 1600
    );

    setTimeout(
        function() {
            document.querySelector("h1.instructions").style.opacity = "1";
        }, 1600
    );

    setTimeout(
        function() {
            document.querySelector(".row").style.opacity = "1";
        }, 1600
    );

    setTimeout(
        function() {
            document.querySelector(".paypal-icon").style.opacity = "1";
        }, 1600
    );


// CONTACT SECTION
    setTimeout(
        function() {
            document.querySelector(".contact-info").style.opacity = "1";
        }, 1600
    );
}